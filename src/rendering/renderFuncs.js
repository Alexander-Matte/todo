export function renderProjects(arr) {
    arr.forEach(element => {
        console.log(element);
    });
}

export function renderTodos(arr) {
    arr.forEach(element => {
        console.log(element);
    });
}

